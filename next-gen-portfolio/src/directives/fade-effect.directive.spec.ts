import { FadeEffectDirective } from './fade-effect.directive';

describe('FadeEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeEffectDirective();
    expect(directive).toBeTruthy();
  });
});
